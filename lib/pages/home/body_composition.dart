import 'package:flutter/material.dart';

class BodyComposition extends StatelessWidget {
  const BodyComposition({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('Body Composition');
  }
}
