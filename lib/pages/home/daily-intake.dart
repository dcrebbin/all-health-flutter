import 'package:flutter/material.dart';

class DailyIntake extends StatelessWidget {
  const DailyIntake({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('Daily Intake');
  }
}
