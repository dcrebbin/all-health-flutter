import 'package:flutter/material.dart';

class Hormones extends StatelessWidget {
  const Hormones({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('Hormones');
  }
}
