import 'package:flutter/material.dart';

class Microbiome extends StatelessWidget {
  const Microbiome({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('Microbiome');
  }
}
