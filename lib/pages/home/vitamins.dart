import 'package:flutter/material.dart';

class Vitamins extends StatelessWidget {
  const Vitamins({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('Vitamins');
  }
}
