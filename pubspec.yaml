name: all_health_flutter
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get_it: ^6.0.0
  injectable: ^1.2.2
  flutter_dotenv: ^5.1.0
  go_router: ^13.2.1

  cupertino_icons: ^1.0.6
  celest: ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - shorebird.yaml